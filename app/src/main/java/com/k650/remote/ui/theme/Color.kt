package com.k650.remote.ui.theme

import androidx.compose.ui.graphics.Color

// ---- Modern dark palette (warm-neutral, amber signature accent) --------------

// Neutrals: deep, slightly warm charcoal rather than flat black.
val Ink          = Color(0xFF0E0F12) // app background
val Surface1     = Color(0xFF16181D) // cards
val Surface2     = Color(0xFF1E2127) // elevated / inputs
val Surface3     = Color(0xFF272B33) // chips, dividers hover
val Outline      = Color(0xFF33383F)
val OnInkHigh    = Color(0xFFECEEF1)
val OnInkMed     = Color(0xFFA8AEB8)
val OnInkFaint   = Color(0xFF6B7280)

// Signature amber (kept for the VU / matrix identity) + a cool secondary.
val Amber        = Color(0xFFFFB020)
val AmberBright  = Color(0xFFFFC65A)
val AmberDeep    = Color(0xFFB77A00)
val AmberGlow    = Color(0x33FFB020)

val Teal         = Color(0xFF43D9C7) // secondary accent (links, "connected")
val Violet       = Color(0xFF8B7CF6) // tertiary accent (services)

// Status
val Good         = Color(0xFF57C77D)
val Warn         = Color(0xFFE0A93B)
val Bad          = Color(0xFFE5534B)

// VU gradient stops
val VuLow        = Color(0xFF57C77D)
val VuMid        = Color(0xFFFFB020)
val VuHigh       = Color(0xFFE5534B)
